package viper.test.sampleapp;
import android.content.Intent
import com.victor.simplechatapp.ChatScreenActivity

class ChatScreenRouter {
    lateinit var activity: ChatScreenActivity
    fun navigateToActivity() {
//        val intent = Intent(activity, TempActivity::class.java)
//        activity.startActivity(intent)
    }
}